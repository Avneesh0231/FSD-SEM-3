# File system of nodeJs
IT aloows JS code rinning outside the browser and intreact directly to operating system

## Common operations on file/folder
-Reading and writing files -> readFile() , writeFile() , appendFile()
-Directory managemnet -> mkdir(), rmdir(), readdir()
-Metadata/information -> stat(), lstat(), fstat()
-Watching for changes -> watch()
-Streaming Large Files -> creteReadStream(), createWriteStream()

CRUD FULL FORM
c = create
r = remove , retrieve
u = update
d = delete